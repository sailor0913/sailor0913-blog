---
interface Props {
  src: string;
  alt: string;
  class?: string;
}

const { src, alt, class: className } = Astro.props;
---

<img
  src={src}
  alt={alt}
  class={`rounded-lg w-full ${className || ''}`}
  loading="lazy"
  decoding="async"
/>
